## enable the below if you want curl to appear more.. browser-y
# user-agent = "Mozilla/5.0 (Windows NT 6.1; rv:45.0) Gecko/20100101 Firefox/45.0"
## make curl automatically set the previous URL when it follows a Location: header.
#referer = ";auto"
## time out faster
connect-timeout = 10
## show a progress bar
progress-bar
## use ipv4, not 6
ipv4
